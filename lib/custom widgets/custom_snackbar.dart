import 'package:flutter/widgets.dart';

class ShowSnackbar extends StatelessWidget {
   ShowSnackbar({super.key, required this.message, required this.isError});
  final String message;
  final bool isError;
  @override
  Widget build(BuildContext context) {
    return Container(
     
    );
  }
}